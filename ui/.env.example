# AI-powered intent classification for the command bar.
# Downloads a ~300MB model (Qwen2.5-0.5B) on first load, cached in IndexedDB after.
# Without this, the command bar uses regex-only parsing (handles most commands).
# VITE_ENABLE_AI_INTENT=true
