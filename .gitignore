# ── Rust / daemon ───────────────────────────────────────────────────────────
daemon/target/
**/*.rs.bk
*.pdb

# ── Node / UI ────────────────────────────────────────────────────────────────
node_modules/
ui/dist/
ui/test-results/
ui/playwright-report/
ui/blob-report/
ui/playwright/.cache/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Environment & secrets ───────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Editor / OS ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# ── Mission Control runtime state ───────────────────────────────────────────
# The daemon writes state + socket here at runtime
.mission-control/

# ── Claude Code local settings (per-worktree, daemon-managed) ───────────────
.claude/settings.local.json
